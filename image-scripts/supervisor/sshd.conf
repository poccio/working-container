[program:sshd]
command=/usr/sbin/sshd -D -e
autostart=true
autorestart=true
# -e sends sshd's log to stderr; route it to the container's stderr so it shows in `docker logs`
stdout_logfile=/dev/fd/1
stdout_logfile_maxbytes=0
stderr_logfile=/dev/fd/2
stderr_logfile_maxbytes=0
